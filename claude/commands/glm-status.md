---
description: "Check background GLM jobs (running / done / failed)"
argument-hint: "[job-id]  (latest if omitted)"
---
Run Bash: `glm-job status $ARGUMENTS` (shows the latest job if no id given). Report the status plainly.
If I ask to see all jobs, run `glm-job list` instead.
